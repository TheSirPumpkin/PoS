using UnityEngine;
using System.Collections;
using System.IO;
using System.Collections.Generic;
using System;
using UnityEngine.UI;

public class AddCMInfoConfirm : MonoBehaviour {
	public CMinfoData CMdata;
	public Text txt;
	public void Void(){
		if (CMdata.member == 1) {
		}
		if (CMdata.member == 2) {
		}
		if (CMdata.member == 3) {
		}
		if (CMdata.member == 4) {
		}
		
		}
	}

